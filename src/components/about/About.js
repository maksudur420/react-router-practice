import React from 'react';
import Header from '../header/Header';

const About = () => {
    return (
        <div>
             <Header></Header>
            <h1>This is About Page Load</h1>
        </div>
    );
};

export default About;